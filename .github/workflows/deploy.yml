name: CI/CD Pipeline

on:
  push:
    branches:
      - main   # Trigger only when main branch gets updated (after PR merge)

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod   # automatically pick version from go.mod

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install dependencies
        run: go mod tidy

      - name: Run tests
        run: go test ./... -v

      - name: Build binary
        run: go build -o build/cncg-bot ./cmd/bot

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cncg-bot
          path: build/cncg-bot

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download built binary
        uses: actions/download-artifact@v4
        with:
          name: cncg-bot
          path: build/

      # Example deployment step (you can replace with SSH, Docker, etc.)
      - name: Deploy to server (placeholder)
        run: |
          echo "Deploying cncg-bot..."
          # scp build/cncg-bot user@your-server:/opt/cncg-bot/
          # ssh user@your-server 'systemctl restart cncg-bot'
